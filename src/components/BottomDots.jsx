import React from 'react'

const BottomDots = ({bg}) => {
  return (
    <div className={`w-2 h-2 bg-${bg} border border-blue mx-1 mb-2 rounded-full `}>
      
    </div>
  )
}

export default BottomDots
